import React, { Component } from 'react'
import Settings from './index'
import { setTitle } from "../../../helpers";

let Interface = () => {
    setTitle('Interface settings');

    return (

        <Settings desc='Interface tuning'>4354</Settings>
    )
};

export default Interface