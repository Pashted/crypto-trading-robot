import React, { Component } from 'react'
import Settings from './index'
import { setTitle } from "../../../helpers";

let Strategy = () => {
    setTitle('Strategy settings');

    return (

        <Settings desc='Strategy adjustment'>6789789</Settings>
    )
};

export default Strategy